name: Build Docker images

on:
  workflow_dispatch:
  # pull_request:
  #   branches:
  #     - main
  #   paths:
  #     - ./docker/**
  push:
    # branches:
    #   - main
    # paths:
    #   - ./docker/**   

jobs:
  docker-build:
    uses: xander-rudolph/mediakube-redux/.github/workflows/docker-publish.yml@pipeline-tweaks
    strategy:
      matrix:
        image: ['pia-vpn']
    with:
      image: ${{ matrix.image }}
      # optional
      working_dir: ./docker/${{ matrix.image }}
    secrets: inherit