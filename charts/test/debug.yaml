apiVersion: v1
kind: Pod
metadata:
  name: redis
spec:
  containers:
  - name: redis
    image: redis
    volumeMounts:
    - name: config-vol
      mountPath: /data/redis
  volumes:
    - name: config-vol
      hostPath:
        path: /mnt/e/docker/redis
        type: DirectoryOrCreate